# ── Secrets (NEVER commit) ───────────────────────────────────────────────────
/secrets/
*.local.yml
*.secret
wifi.pass
mqtt.pass

# ── Local profile (generated by `make profile PROFILE=…` if not committed) ──
/profiles/current.yml

# ── Python ───────────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# ── PlatformIO / ESP32 ───────────────────────────────────────────────────────
firmware/**/.pio/
firmware/**/.pioenvs/
firmware/**/.piolibdeps/
firmware/**/lib_deps_external/

# ── IDE ──────────────────────────────────────────────────────────────────────
.vscode/c_cpp_properties.json
.vscode/launch.json
.vscode/ipch/
.idea/
*.swp
.DS_Store

# ── Claude / local notes ─────────────────────────────────────────────────────
.claude/
NOTES.local.md

# ── Build artefacts ──────────────────────────────────────────────────────────
build/
dist/
*.bak
*.backup
*.corrupted
